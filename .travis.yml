language: ruby
rvm:
  - 2.4.0
env:
  - DB=pgsql
services:
  - postgresql
script:
  - RAILS_ENV=test bundle exec rake db:migrate --trace
  - bin/rake test
install: gem install rainbow -v '2.2.1'
test:
  adapter: postgresql
  database: flashcards_test
before_script:
  - cp config/database.yml.travis config/database.yml
